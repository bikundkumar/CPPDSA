#include <map>
#include <iostream>
using namespace std;

void Multi_Map() {
    // everything same as map, only it can store multiple keys
    // only mpp[key] cannot be used here
}

int main() {
    Multi_Map();
}